import {checkForName} from "../src/client/js/nameChecker";

describe("defention test for name check function", () => {
    test("test if checkForName() is defined or not", () => {
           expect(checkForName).toBeDefined();
})});