import {handleSubmit} from '../src/client/js/formHandler'


describe("defention test for submition function", () => {
    test("test if handleSubmit() is defined or not", () => {
        expect(handleSubmit).toBeDefined();       
})});